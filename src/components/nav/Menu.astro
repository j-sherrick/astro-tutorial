---
interface Props {
    class?: string;
}
const { class: className } = Astro.props;
---

<menu class:list={[className]}>
    <slot />
</menu>

<style lang="scss">

    menu {

    }

    .dropdown {
        display: none;
        position: absolute;
    }

    .navigation {
        display: flex;
        align-items: center;
        position: relative;
        gap: 2rem;
    }
    
</style>
